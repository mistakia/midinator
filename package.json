{
  "name": "midinator",
  "version": "0.0.1",
  "description": "midi video generator",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "start:watch": "NODE_ENV=development nodemon --watch 'main.js' --watch 'renderer.js' --watch 'config.js' --watch './src/**/*.js' --watch *.html --watch *.css --exec 'electron .'"
  },
  "devDependencies": {
    "electron": "^4.1.1",
    "nodemon": "^1.18.10"
  },
  "dependencies": {
    "@simonwep/pickr": "^0.4.4",
    "canvas-to-buffer": "^1.0.14",
    "d3-ease": "^1.0.5",
    "electron-default-menu": "^1.0.1",
    "electron-osx-prompt": "^1.4.1",
    "electron-prompt": "^1.3.1",
    "fluent-ffmpeg": "^2.1.2",
    "howler": "^2.1.1",
    "jsonfile": "^5.0.0",
    "leftpad": "0.0.1",
    "midi-player-js": "^2.0.5",
    "rimraf": "^2.6.3"
  }
}
