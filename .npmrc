# Defense-in-depth against npm supply-chain attacks: refuse to run install
# lifecycle scripts. CI doesn't read the user-home ~/.npmrc, so this project-
# level file enforces the same posture inside CI runners. Override with
# `ignore-scripts=false` only when a legitimate native build requires it.
ignore-scripts=true
